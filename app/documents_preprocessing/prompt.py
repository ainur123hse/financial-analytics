def make_chart_detection_prompt(text_before: str, text_after: str) -> str:
    return f"""Тебе будет предоставлено изображение внутри markdown документа, часть текста до него и часть после него.
Твоя задача:
1. Определить, является ли основное содержимое изображения графиком, диаграммой или иным визуальным представлением данных, которое имеет смысл пытаться восстановить в таблицу.
2. Вернуть строгий JSON-объект без markdown fences и без поясняющего текста вокруг.

Формат ответа:
{{
  "kind": "chart" | "other",
  "description": string | null
}}

Правила:
- Возвращай kind="chart" только если изображение действительно выглядит как график, диаграмма или визуализация данных.
- Если это логотип, фото, декоративная иллюстрация, иконка, скриншот интерфейса, служебный элемент или иной не-график, возвращай kind="other".
- Поле description заполняй всегда: 1-2 коротких предложения по существу.
- Не выдумывай скрытые данные и не пытайся восстанавливать таблицу на этом шаге.

Часть текста до изображения:
```{text_before}```

Часть текста после изображения:
```{text_after}```"""


def make_structured_prompt(text_before: str, text_after: str) -> str:
    return f"""Тебе будет предоставлено изображение внутри markdown документа, часть текста до него и часть после него.
Изображение уже предварительно классифицировано как график или диаграмма.
Твоя задача:
1. Преобразовать этот график в структуру, пригодную для рендера markdown-таблицы.
2. Вернуть строгий JSON-объект без markdown fences и без поясняющего текста вокруг.

Формат ответа:
{{
  "kind": "chart",
  "title": string | null,
  "description": string | null,
  "columns": [string, ...],
  "rows": [[string, ...], ...],
  "approximate": boolean
}}

Правила:
- Всегда возвращай kind="chart".
- Для chart первая колонка должна быть осью X или категориями, остальные колонки должны быть отдельными рядами данных.
- Обязательно сохраняй единицы измерения в заголовках колонок, если они видны на изображении.
- Если значение читается не точно, но его можно оценить, укажи его как строку с префиксом "≈".
- Нельзя выдумывать скрытые данные. Если таблицу надежно восстановить нельзя, оставь columns и rows пустыми.
- Поле description заполняй всегда: 1-2 коротких предложения о том, что изображено.

Часть текста до изображения:
```{text_before}```

Часть текста после изображения:
```{text_after}```"""


def make_description_prompt(text_before: str, text_after: str) -> str:
    return f"""Тебе будет предоставлено изображение внутри markdown документа, часть текста до него и часть после него.
Твоя задача лаконично, в 1-2 предложениях, описать изображение по существу.
Не используй markdown и не упоминай, что у тебя был промпт.

Часть текста до изображения:
```{text_before}```

Часть текста после изображения:
```{text_after}```"""
